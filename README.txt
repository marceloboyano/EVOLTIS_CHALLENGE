# EVOLTIS CHALLENGE

Es una aplicación con WebForms que tiene un Crud de empleados y una busqueda, los datos son guardados en una base de datos SQL.

## Requisitos Previos


- Antes de empezar deje un script en la raiz del repositorio para poder crear la base de datos rapidamente.
- Hay que presionar doble click sobre el script, se abrira una ventana en sql management studio o elija este por defecto
   y presionar ejecutar o F5 para correr el script.

## Instalación

1. Clonar este repositorio: `git clone https://github.com/tu-usuario/tu-repositorio.git`
2. Navegar a la carpeta del proyecto: `cd nombre-de-la-carpeta`
3. Instalar dependencias: `npm install` o `pip install -r requirements.txt`, dependiendo del lenguaje

## Configuración

Si es necesario, describe cómo configurar tu aplicación:

1. Crear un archivo de configuración: `cp config.example.json config.json`
2. Editar `config.json` y agregar las configuraciones necesarias, como claves API

## Uso

Explica cómo utilizar tu aplicación una vez que esté instalada y configurada:

1. Ejecutar la aplicación: presionando F5.
2. Se abrira el navegador que usa por defecto.


